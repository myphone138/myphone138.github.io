git init

git config user.name "myphone138"

git config user.name "myphone138@outlook.com"

git config user.signingkey "G:\keys\git138\myphone138_0x3C2A2DA7_SECRET.asc"

git config commit.gpgsign true

git config  user.signingkey E0139AF57652F8D548EB1312C9BDB14C3C2A2DA7

 git config --local --get user.signingkey

echo "禁止推送/" > .gitignoregit add .gitignoregit commit -m "Add .gitignore to exclude 禁止推送 directory"

git remote add origin git@github.com:myphone138/myphone138.github.io.git

ssh -T git@github.com

git push -f origin main


Copilot：
Remove-Item -Path "C:/Users/mypho/.gnupg/gnupg_spawn_keyboxd_sentinel.lock" -Force; git commit -S -m "Initial commit" && git push -f origin main


git config --local core.sshCommand "ssh -i G:/keys/git138/id_rsa_4096_138" && git push -f origin main

